import React from 'react';

const WaitingForApproval = () => {
  return (
    <div>
      <h2>Your receipt is pending approval.</h2>
      <p>Please wait for an admin to review and confirm your receipt. You will be notified once it is approved.</p>
    </div>
  );
};

export default WaitingForApproval;
